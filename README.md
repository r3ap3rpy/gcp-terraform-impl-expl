### Welcome

This is a demo terraform based GCP project.

It shows implicit and explicit resource creation.

Dependencies:
- terraform
- gcloud cli

To setup you need to run the following commands.

``` bash
terraform init
terraform plan
terraform apply --auto-approve
```
